# shellcheck shell=bash
# Configuration for rel-phosh-component
#
# To be placed in ~/.config/

# Name of the git remote to push tags to
#GIT_REMOTE=
# Remote host to push releases to
#REMOTE_HOST=
# Remote path to push releases to
#REMOTE_PATH=
# Name of the pass token
# GITLAB_TOKEN_PASS=
#MK_GITLAB_REL=/path/to/mk-gitlab-rel.py

# If you release on different instances you can do it
# like:
#case ${COMPONENT} in
#   cellbroadcastd)
#       GIT_REMOTE=
#       GITLAB_TOKEN_PASS=
#       ;;
#esac
